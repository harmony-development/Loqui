use super::prelude::*;

#[derive(Default)]
pub struct Screen {}

impl AppScreen for Screen {
    fn update(&mut self, ctx: &egui::Context, frame: &epi::Frame, state: &mut State) {
        todo!()
    }
}
